package ch.hsr.adv.commons.core.logic.domain;

/**
 * This enum is used to define where a module should be displayed in the ui,
 * this is relevant for the positioning of childModules when you want them
 * at a specific position
 */
public enum ModulePosition {
    DEFAULT, LEFT, TOP, RIGHT, BOTTOM
}
